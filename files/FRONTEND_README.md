# 🎨 Frontend пакет: Фото еды и консультации

## 📦 Что в пакете (10 файлов)

### ✨ Hooks - React хуки (2 файла)
1. **useDailyFood.ts** - работа с фото еды и калориями
2. **useNutritionAdvice.ts** - консультации нутрициолога

### 🎨 Components - React компоненты (4 файла)
3. **NutritionApp.tsx** - главный компонент с табами
4. **DailyFoodTracker.tsx** - дневник питания (фото + калории)
5. **FoodHistory.tsx** - история по дням со статистикой
6. **NutritionAdvicePanel.tsx** - панель консультаций

### 💅 Styles - CSS стили (4 файла)
7. **NutritionApp.css** - стили главного компонента
8. **DailyFoodTracker.css** - стили дневника
9. **FoodHistory.css** - стили истории
10. **NutritionAdvicePanel.css** - стили консультаций

---

## 🚀 Быстрая установка за 3 шага

### Шаг 1: Скопировать файлы

```bash
frontend/src/
├── hooks/
│   ├── useDailyFood.ts                    ← скопировать
│   └── useNutritionAdvice.ts              ← скопировать
├── components/
│   ├── NutritionApp.tsx                   ← скопировать
│   ├── DailyFoodTracker.tsx               ← скопировать
│   ├── FoodHistory.tsx                    ← скопировать
│   ├── NutritionAdvicePanel.tsx           ← скопировать
│   ├── NutritionApp.css                   ← скопировать
│   ├── DailyFoodTracker.css               ← скопировать
│   ├── FoodHistory.css                    ← скопировать
│   └── NutritionAdvicePanel.css           ← скопировать
```

### Шаг 2: Подключить в App.tsx

```typescript
import { NutritionApp } from './components/NutritionApp';

function App() {
  const token = localStorage.getItem('token');

  return (
    <div className="App">
      {token ? (
        <NutritionApp token={token} />
      ) : (
        <LoginForm />
      )}
    </div>
  );
}

export default App;
```

### Шаг 3: Готово! 🎉

Запустите приложение:
```bash
npm start
```

---

## 💡 Что вы получаете

### 1. Дневник питания с AI

```
┌───────────────────────────────────┐
│ 📸 Дневник питания                │
│ ← [2025-11-07] → [Сегодня]       │
├───────────────────────────────────┤
│ Всего калорий за день: 1850 ккал  │
├───────────────────────────────────┤
│ ⏰ 08:30                          │
│ Овсянка с фруктами                │
│ Овсянка на молоке с бананом       │
│ 🔥 350 ккал                        │
├───────────────────────────────────┤
│ ⏰ 13:00                          │
│ Куриная грудка с овощами          │
│ Запеченная курица, брокколи       │
│ 🔥 420 ккал                        │
├───────────────────────────────────┤
│ [📸 Загрузить фото]               │
│ [✏️ Добавить вручную]             │
└───────────────────────────────────┘
```

**Возможности:**
- ✅ Загрузка фото → AI определяет блюдо и калории
- ✅ Автоматический подсчет суммы за день
- ✅ Ручной ввод (без фото)
- ✅ Навигация по дням
- ✅ Удаление записей

### 2. История со статистикой

```
┌───────────────────────────────────┐
│ 📊 История питания                │
│ ┌─────────┬─────────┬─────────┐  │
│ │ Дней: 30│ Средняя │ Всего   │  │
│ │         │ 1820    │ 54,600  │  │
│ └─────────┴─────────┴─────────┘  │
├───────────────────────────────────┤
│ 🔵 Сегодня         2025-11-07    │
│ 📸 3 фото          🔥 1850 ккал  │
│ [████████░░] 74%                  │
├───────────────────────────────────┤
│ ⚪ Вчера           2025-11-06    │
│ 📸 4 фото          🔥 2100 ккал  │
│ [██████████] 84%                  │
└───────────────────────────────────┘
```

**Возможности:**
- ✅ История за 7-90 дней
- ✅ Статистика (всего дней, средняя, сумма)
- ✅ Клик → переход к дню
- ✅ Визуальная шкала калорий

### 3. Консультации нутрициолога

```
┌───────────────────────────────────┐
│ 💡 Консультации нутрициолога      │
│ ← [2025-11-07] → [Сегодня]       │
├───────────────────────────────────┤
│ 2 консультации                    │
├───────────────────────────────────┤
│ ⏰ 10:15  🎯 Снижение холестерина │
│                                   │
│ Для эффективного снижения         │
│ холестерина рекомендую:           │
│ 1. Увеличить потребление...       │
│ 2. Ограничить насыщенные жиры...  │
├───────────────────────────────────┤
│ [✨ Получить консультацию]        │
└───────────────────────────────────┘
```

**Возможности:**
- ✅ Консультации по дням
- ✅ AI-рекомендации на основе профиля
- ✅ Фокус консультации (опционально)
- ✅ История консультаций

---

## 🎯 Примеры использования

### Пример 1: Загрузить фото завтрака

```typescript
import { useDailyFood } from './hooks/useDailyFood';

function MyComponent() {
  const { addPhoto } = useDailyFood(token);

  const handleUpload = async (file: File) => {
    try {
      const result = await addPhoto(
        file,
        '2025-11-07',
        '08:30:00',
        'Завтрак'
      );
      
      console.log('AI определил:', result.title);
      console.log('Калории:', result.estimated_calories);
    } catch (err) {
      console.error(err);
    }
  };

  return <input type="file" onChange={e => handleUpload(e.target.files[0])} />;
}
```

### Пример 2: Получить день с суммой калорий

```typescript
import { useDailyFood } from './hooks/useDailyFood';

function MyComponent() {
  const { getDay } = useDailyFood(token);

  useEffect(() => {
    const loadDay = async () => {
      const data = await getDay('2025-11-07');
      
      console.log('Всего калорий:', data.total_calories);
      console.log('Приемов пищи:', data.photos.length);
      
      data.photos.forEach(photo => {
        console.log(`${photo.time}: ${photo.title} - ${photo.estimated_calories} ккал`);
      });
    };
    
    loadDay();
  }, []);
}
```

### Пример 3: Создать консультацию

```typescript
import { useNutritionAdvice } from './hooks/useNutritionAdvice';

function MyComponent() {
  const { createAdvice } = useNutritionAdvice(token);

  const getAdvice = async () => {
    const result = await createAdvice(
      '2025-11-07',
      'Снижение холестерина'
    );
    
    console.log('Консультация:', result.advice);
  };

  return <button onClick={getAdvice}>Получить консультацию</button>;
}
```

---

## 🎨 Адаптивный дизайн

Все компоненты полностью адаптивны:

- ✅ Desktop (> 768px) - полная ширина
- ✅ Tablet (768px) - адаптированная сетка
- ✅ Mobile (< 768px) - вертикальная компоновка

**Брейкпоинт:** `768px`

---

## 🔧 Настройка

### Изменить API endpoint

По умолчанию: `/api/*`

Для изменения создайте `config.ts`:

```typescript
export const API_URL = process.env.REACT_APP_API_URL || '/api';

// В хуках:
fetch(`${API_URL}/daily-food/${date}`, ...)
```

### Изменить цвета

В CSS файлах найдите градиенты:

```css
/* Фиолетовый (основной) */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* Зеленый (консультации) */
background: linear-gradient(135deg, #27ae60 0%, #229954 100%);
```

Замените на свои:

```css
/* Синий */
background: linear-gradient(135deg, #3498db 0%, #2980b9 100%);
```

---

## 📊 TypeScript поддержка

Все типы экспортируются:

```typescript
// Фото еды
import type { 
  FoodPhoto,        // Одно фото
  DayData,          // День с суммой
  DayHistory,       // День в истории
  ManualFoodData    // Ручной ввод
} from './hooks/useDailyFood';

// Консультации
import type { 
  NutritionAdvice,  // Одна консультация
  AdviceDay,        // День с консультациями
  AdviceHistory     // День в истории
} from './hooks/useNutritionAdvice';
```

---

## ⚡ Производительность

### Оптимизации:
- ✅ React.memo для компонентов
- ✅ useCallback для функций
- ✅ Ленивая загрузка истории
- ✅ Дебаунс для поиска (если добавите)

### Рекомендации:
```typescript
// Мемоизация компонента
export const DailyFoodTracker = React.memo(({ token }) => {
  // ...
});

// Мемоизация вычислений
const totalCalories = useMemo(
  () => photos.reduce((sum, p) => sum + (p.estimated_calories || 0), 0),
  [photos]
);
```

---

## 🧪 Тестирование

### Пример теста для хука:

```typescript
import { renderHook } from '@testing-library/react-hooks';
import { useDailyFood } from './useDailyFood';

test('должен загрузить день', async () => {
  const { result } = renderHook(() => useDailyFood('test-token'));
  
  const data = await result.current.getDay('2025-11-07');
  
  expect(data.total_calories).toBeGreaterThan(0);
  expect(data.photos).toBeInstanceOf(Array);
});
```

---

## 📚 Документация

- **[FRONTEND_DOCUMENTATION.md](computer:///mnt/user-data/outputs/FRONTEND_DOCUMENTATION.md)** - полная документация

---

## ✅ Чеклист

- [ ] Скопировали все 10 файлов
- [ ] Подключили в App.tsx
- [ ] Проверили компиляцию TypeScript
- [ ] Протестировали на desktop
- [ ] Протестировали на mobile
- [ ] Настроили API endpoint (если нужно)
- [ ] Кастомизировали стили (если нужно)

---

## 🎉 Готово!

Теперь у вас есть **полноценный frontend**:
- ✅ 2 React хука
- ✅ 4 React компонента
- ✅ 4 CSS файла
- ✅ TypeScript типы
- ✅ Адаптивный дизайн
- ✅ Готовая документация

**Приятного использования! 🍽️**
