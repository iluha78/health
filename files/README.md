# CholestoFit: Обновление для хранения всех данных в базе

## 🎯 Цель обновления

Все данные приложения теперь хранятся в базе данных, а не в браузере. Это обеспечивает:

- ✅ **Сохранность данных** - данные не теряются при очистке браузера
- ✅ **Мультиустройственность** - доступ к данным с любого устройства
- ✅ **Надежность** - резервное копирование и восстановление
- ✅ **Аналитика** - возможность анализа истории и трендов

## 📦 Содержимое пакета

```
.
├── README.md                              # Этот файл
├── INSTALLATION_GUIDE.md                  # Подробная инструкция
├── CHANGES_SUMMARY.md                     # Сводка изменений и примеры кода
├── install.sh                             # Скрипт автоматической установки
├── backend-blood-pressure-update.tar.gz   # Архив с backend файлами
└── [Отдельные файлы для ручной установки]
    ├── backend_migrations_004_blood_pressure.sql
    ├── backend_BloodPressureRecord.php
    ├── backend_BloodPressureController.php
    └── routes.php
```

## 🚀 Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# 1. Скопируйте все файлы в корень проекта
cd /path/to/your/cholestofit-project

# 2. Запустите скрипт установки
./install.sh

# 3. Следуйте инструкциям на экране
```

### Вариант 2: Ручная установка

```bash
# 1. Распакуйте архив
tar -xzf backend-blood-pressure-update.tar.gz

# 2. Скопируйте файлы
cp backend_migrations_004_blood_pressure.sql backend/migrations/004_blood_pressure.sql
cp backend_BloodPressureRecord.php backend/src/Models/BloodPressureRecord.php
cp backend_BloodPressureController.php backend/src/Controllers/BloodPressureController.php
cp routes.php backend/config/routes.php

# 3. Обновите автозагрузку
cd backend
composer dump-autoload

# 4. Примените миграцию
composer migrate

# Или если используете Docker:
cd ..
docker-compose restart backend
```

## 🔧 Что изменилось

### Новая функциональность

**Измерения давления, пульса и сахара**
- Эндпоинт: `GET/POST/DELETE /blood-pressure`
- Таблица: `blood_pressure_records`
- Хранит: систолическое/диастолическое давление, пульс, сахар, заметки

### Что уже работало (без изменений)

- ✅ Липидный профиль (`/lipids`)
- ✅ Профиль пользователя (`/profile`, `/targets`)
- ✅ Консультации нутрициолога (`/advice/nutrition`)
- ✅ AI-ассистент (`/assistant/chat`)
- ✅ Анализ фото (`/analysis/photo`)
- ✅ Дневник питания (`/diary`)

## 📚 Документация

### Для backend разработчиков

**INSTALLATION_GUIDE.md** содержит:
- Подробную структуру таблицы
- Описание всех API endpoints
- Примеры запросов и ответов
- Решение типичных проблем

### Для frontend разработчиков

**CHANGES_SUMMARY.md** содержит:
- Готовые React хуки для работы с API
- Примеры компонентов
- TypeScript типы
- Рекомендации по интеграции

## 🧪 Проверка работоспособности

### 1. Проверка миграции

```bash
mysql -u root -p cholestofit -e "SHOW TABLES LIKE 'blood_pressure_records';"
```

Ожидаемый результат:
```
+----------------------------------------------------+
| Tables_in_cholestofit (blood_pressure_records)     |
+----------------------------------------------------+
| blood_pressure_records                             |
+----------------------------------------------------+
```

### 2. Проверка структуры

```bash
mysql -u root -p cholestofit -e "DESCRIBE blood_pressure_records;"
```

### 3. Тест API

```bash
# Замените YOUR_TOKEN на реальный JWT токен
curl -X GET \
  http://localhost:8080/blood-pressure \
  -H "Authorization: Bearer YOUR_TOKEN"
```

Ожидаемый результат: `[]` или список записей

### 4. Тест создания записи

```bash
curl -X POST \
  http://localhost:8080/blood-pressure \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "measured_at": "2025-11-02T10:30:00Z",
    "systolic": 120,
    "diastolic": 80,
    "pulse": 72,
    "note": "Тестовая запись"
  }'
```

## 🔍 Структура новой таблицы

```sql
CREATE TABLE blood_pressure_records (
  id INT AUTO_INCREMENT PRIMARY KEY,
  user_id INT NOT NULL,
  measured_at DATETIME NOT NULL,
  systolic INT,              -- Систолическое (верхнее)
  diastolic INT,             -- Диастолическое (нижнее)
  pulse INT,                 -- Пульс
  glucose DECIMAL(5,2),      -- Сахар
  note VARCHAR(500),         -- Заметка
  created_at TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

## 📊 API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/blood-pressure` | Получить все измерения |
| POST | `/blood-pressure` | Создать новое измерение |
| DELETE | `/blood-pressure/{id}` | Удалить измерение |

Все endpoints требуют JWT авторизации через заголовок `Authorization: Bearer {token}`

## 🎨 Пример использования во frontend

```typescript
// Сохранение измерения
const saveBloodPressure = async (data) => {
  const response = await fetch('/api/blood-pressure', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      measured_at: new Date().toISOString(),
      systolic: 120,
      diastolic: 80,
      pulse: 72,
      note: 'После тренировки'
    })
  });
  
  return response.json();
};

// Получение истории
const loadHistory = async () => {
  const response = await fetch('/api/blood-pressure', {
    headers: {
      'Authorization': `Bearer ${token}`
    }
  });
  
  return response.json();
};
```

Подробные примеры см. в **CHANGES_SUMMARY.md**

## 🔄 Миграция существующих данных

Если у вас есть данные в localStorage, их нужно перенести в базу:

```javascript
// Пример скрипта миграции (выполнить в консоли браузера)
const oldData = JSON.parse(localStorage.getItem('blood_pressure_history') || '[]');

for (const record of oldData) {
  await fetch('/api/blood-pressure', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${yourToken}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      measured_at: record.date,
      systolic: record.systolic,
      diastolic: record.diastolic,
      pulse: record.pulse,
      note: record.note
    })
  });
}

console.log('Миграция завершена!');
```

## ⚠️ Важные замечания

1. **Резервные копии**: Скрипт установки автоматически создает резервные копии в `backend/backups/`
2. **Composer autoload**: Обязательно выполните `composer dump-autoload` после копирования файлов
3. **Frontend**: Обновите код для использования API вместо localStorage
4. **Безопасность**: Все endpoints защищены JWT авторизацией

## 🐛 Решение проблем

### "Table already exists"

```sql
DROP TABLE IF EXISTS blood_pressure_records;
```

Затем снова запустите миграцию.

### "Class not found"

```bash
cd backend
composer dump-autoload
```

### API возвращает 401

Проверьте наличие и валидность JWT токена в заголовке запроса.

### Данные не сохраняются

Проверьте логи:
```bash
docker-compose logs -f backend
# или
tail -f backend/logs/error.log
```

## 📞 Поддержка

- 📖 Полная документация: `INSTALLATION_GUIDE.md`
- 💻 Примеры кода: `CHANGES_SUMMARY.md`
- 🔧 Проблемы и вопросы: создайте issue в репозитории

## 📋 Чеклист после установки

- [ ] Миграция применилась успешно
- [ ] Таблица `blood_pressure_records` создана
- [ ] API endpoints отвечают корректно
- [ ] Frontend обновлен для работы с API
- [ ] Код работы с localStorage удален
- [ ] Тестирование функциональности пройдено
- [ ] Резервные копии созданы

## 🎉 Готово!

После установки все измерения давления, пульса и сахара будут автоматически сохраняться в базе данных и доступны с любого устройства.

**Следующие шаги:**
1. Обновите frontend код (см. CHANGES_SUMMARY.md)
2. Протестируйте функциональность
3. Проведите миграцию старых данных из localStorage (если есть)
4. Наслаждайтесь надежным хранением данных! 🎊
